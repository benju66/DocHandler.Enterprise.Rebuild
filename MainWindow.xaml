<Window x:Class="DocHandler.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:vm="clr-namespace:DocHandler.ViewModels"
        mc:Ignorable="d"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="DocHandler Enterprise" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Closing="MainWindow_Closing">
    
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    
    <DockPanel>
        <!-- Menu Bar -->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Set Save Location..." Command="{Binding SetSaveLocationCommand}">
                    <MenuItem.Icon>
                        <ui:SymbolIcon Symbol="Folder"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="E_xit" Command="{Binding ExitCommand}">
                    <MenuItem.Icon>
                        <ui:SymbolIcon Symbol="Cancel"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            
            <MenuItem Header="_Processing">
                <MenuItem Header="Save Quotes Mode" 
                          IsCheckable="True" 
                          IsChecked="{Binding SaveQuotesMode}">
                    <MenuItem.Icon>
                        <ui:SymbolIcon Symbol="Save"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="Edit Company Names..." Command="{Binding EditCompanyNamesCommand}"/>
                <MenuItem Header="Edit Scopes of Work..." Command="{Binding EditScopesOfWorkCommand}"/>
            </MenuItem>
            
            <MenuItem Header="_View">
                <MenuItem Header="Dark Mode" IsCheckable="True" IsChecked="{Binding IsDarkMode}">
                    <MenuItem.Icon>
                        <ui:SymbolIcon Symbol="WebCam"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            
            <MenuItem Header="_Help">
                <MenuItem Header="_About..." Command="{Binding AboutCommand}">
                    <MenuItem.Icon>
                        <ui:SymbolIcon Symbol="Help"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
        </Menu>
        
        <!-- Status Bar -->
        <StatusBar DockPanel.Dock="Bottom" Height="25">
            <StatusBarItem>
                <TextBlock>
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="Normal Mode"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SaveQuotesMode}" Value="True">
                                    <Setter Property="Text" Value="Save Quotes Mode"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="{DynamicResource SystemControlHighlightAccentBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock>
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding SaveQuotesMode}" Value="True"/>
                                        <Condition Binding="{Binding SelectedScope, Converter={StaticResource StringNotEmptyConverter}}" Value="True"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Setter Property="Text">
                                        <Setter.Value>
                                            <MultiBinding StringFormat="{}Selected: {0}">
                                                <Binding Path="SelectedScope"/>
                                            </MultiBinding>
                                        </Setter.Value>
                                    </Setter>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StatusBarItem>
        </StatusBar>
        
        <!-- Progress Bar -->
        <ProgressBar DockPanel.Dock="Bottom" 
                     Height="4" 
                     Value="{Binding ProgressValue}"
                     Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisibilityConverter}}"/>
        
        <!-- Main Content Area -->
        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition>
                    <ColumnDefinition.Style>
                        <Style TargetType="ColumnDefinition">
                            <Setter Property="Width" Value="350"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SaveQuotesMode}" Value="False">
                                    <Setter Property="Width" Value="0"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ColumnDefinition.Style>
                </ColumnDefinition>
            </Grid.ColumnDefinitions>
            
            <!-- Main Panel: Drop Zone and Normal Mode -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Drop Zone -->
                <Border Grid.Row="0" 
                        Name="DropBorder"
                        BorderThickness="2" 
                        CornerRadius="10"
                        AllowDrop="True"
                        Drop="Border_Drop"
                        DragEnter="Border_DragEnter"
                        DragLeave="Border_DragLeave"
                        Margin="5">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="BorderBrush" Value="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
                            <Setter Property="Background" Value="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SaveQuotesMode}" Value="True">
                                    <Setter Property="BorderBrush" Value="{DynamicResource SystemControlHighlightAccentBrush}"/>
                                    <Setter Property="Background" Value="{DynamicResource SystemControlBackgroundChromeMediumBrush}"/>
                                </DataTrigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="BorderBrush" Value="{DynamicResource SystemControlHighlightAccentBrush}"/>
                                    <Setter Property="Background" Value="{DynamicResource SystemControlBackgroundListLowBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    
                    <Grid>
                        <!-- Drop Instructions (when no files) -->
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                                    Visibility="{Binding Path=PendingFiles.Count, Converter={StaticResource IntToVisibilityConverter}, ConverterParameter=0}">
                            <ui:SymbolIcon Symbol="OpenFile" Width="48" Height="48" 
                                          Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
                            <TextBlock Text="{Binding StatusMessage}" 
                                      TextAlignment="Center" 
                                      Margin="10" 
                                      FontSize="16"
                                      TextWrapping="Wrap"
                                      MaxWidth="400"/>
                            <TextBlock TextAlignment="Center" 
                                      FontSize="12"
                                      Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="Drop PDFs, Word documents, or Excel files here"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SaveQuotesMode}" Value="True">
                                                <Setter Property="Text" Value="Drop quote documents here"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                        
                        <!-- Pending Files List -->
                        <ListBox ItemsSource="{Binding PendingFiles}" 
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 Margin="10">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource SystemControlBackgroundListLowBrush}"
                                            CornerRadius="4" Padding="10" Margin="0,2">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <TextBlock Grid.Column="0" Text="📄" FontSize="24" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                            
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding FileName}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                                <TextBlock FontSize="12" Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}">
                                                    <TextBlock.Text>
                                                        <MultiBinding StringFormat="{}{0} • {1}">
                                                            <Binding Path="FileType"/>
                                                            <Binding Path="FileSizeDisplay"/>
                                                        </MultiBinding>
                                                    </TextBlock.Text>
                                                </TextBlock>
                                            </StackPanel>
                                            
                                            <Button Grid.Column="3" 
                                                    Content="✕" 
                                                    FontSize="16"
                                                    Width="30" Height="30"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Command="{Binding DataContext.RemoveFileCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"
                                                    ToolTip="Remove file"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>
                
                <!-- Process Button -->
                <Button Grid.Row="1" 
                        Command="{Binding ProcessFilesCommand}"
                        IsEnabled="{Binding CanProcess}"
                        HorizontalAlignment="Center"
                        Margin="5"
                        Padding="20,10"
                        FontSize="14"
                        Style="{StaticResource AccentButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Play" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding ProcessButtonText}"/>
                    </StackPanel>
                </Button>
            </Grid>
            
            <!-- Right Panel: Save Quotes Mode Controls -->
            <Grid Grid.Column="1" Margin="10,0,0,0">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SaveQuotesMode}" Value="False">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Company Name Section -->
                <GroupBox Grid.Row="0" Header="Company Name" Margin="0,0,0,10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <TextBox Grid.Row="0"
                                 x:Name="CompanyNameTextBox"
                                 Text="{Binding CompanyNameInput, UpdateSourceTrigger=PropertyChanged}"
                                 ui:ControlHelper.PlaceholderText="{Binding DetectedCompanyName}"
                                 FontSize="14"
                                 Padding="8,6"
                                 Margin="0,5,0,0">
                            <TextBox.Style>
                                <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsDetectingCompany}" Value="True">
                                            <Setter Property="IsEnabled" Value="False"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                        
                        <TextBlock Grid.Row="1" 
                                   Text="Company name will be detected from document"
                                   FontSize="11"
                                   Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                                   Margin="0,5,0,0"
                                   TextWrapping="Wrap">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Path=PendingFiles.Count, Converter={StaticResource IntToBoolConverter}}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </GroupBox>
                
                <!-- Scope of Work Section -->
                <GroupBox Grid.Row="1" Header="Scope of Work" Margin="0,0,0,5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Search Box -->
                        <Grid Grid.Row="0" Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ui:AutoSuggestBox Grid.Column="0" 
                                              PlaceholderText="Search scopes..."
                                              Text="{Binding ScopeSearchText, UpdateSourceTrigger=PropertyChanged}"
                                              QueryIcon="Find"/>
                            <Button Grid.Column="1" 
                                    Margin="5,0,0,0"
                                    Command="{Binding SearchScopesCommand}"
                                    ToolTip="Search">
                                <ui:SymbolIcon Symbol="Find" Width="16" Height="16"/>
                            </Button>
                        </Grid>
                        
                        <!-- Selected Scope Display -->
                        <Border Grid.Row="1" 
                                Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}"
                                BorderBrush="{DynamicResource SystemControlHighlightAccentBrush}"
                                BorderThickness="2"
                                CornerRadius="4"
                                Padding="10,8"
                                Margin="0,5">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedScope, Converter={StaticResource StringNotEmptyConverter}}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="{Binding SelectedScope}" 
                                       FontWeight="SemiBold"
                                       TextWrapping="Wrap"/>
                        </Border>
                    </Grid>
                </GroupBox>
                
                <!-- Scopes List -->
                <GroupBox Grid.Row="2" Header="Available Scopes">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <ListBox Grid.Row="0"
                                 ItemsSource="{Binding ScopesOfWork}"
                                 SelectedItem="{Binding SelectedScope}"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                                    <EventSetter Event="Selected" Handler="ScopeItem_Selected"/>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" 
                                              Padding="5,3"
                                              ToolTip="{Binding}"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        
                        <!-- Recent Scopes -->
                        <Expander Grid.Row="1" 
                                  Header="Recent Scopes" 
                                  Margin="0,5,0,0"
                                  IsExpanded="False">
                            <ListBox ItemsSource="{Binding RecentScopes}"
                                     MaxHeight="150"
                                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                                     BorderThickness="0">
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                                        <Setter Property="Cursor" Value="Hand"/>
                                        <EventSetter Event="MouseDoubleClick" Handler="RecentScopeItem_DoubleClick"/>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" 
                                                  Padding="3,2"
                                                  FontSize="12"
                                                  ToolTip="Double-click to select"/>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Expander>
                    </Grid>
                </GroupBox>
                
                <!-- Session Save Location -->
                <StackPanel Grid.Row="3" Margin="0,10,0,0">
                    <TextBlock Text="Save Location:" 
                               FontSize="12"
                               Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
                    <TextBlock Text="{Binding SessionSaveLocation}" 
                               FontSize="11"
                               TextTrimming="CharacterEllipsis"
                               ToolTip="{Binding SessionSaveLocation}"/>
                </StackPanel>
            </Grid>
        </Grid>
    </DockPanel>
</Window>